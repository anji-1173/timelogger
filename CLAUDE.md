# CLAUDE.md

このリポジトリで Claude が守るルールです。

## プロジェクト概要

自分専用のタイムロガー。単一 HTML ファイル(`index.html`)で動作し、記録はブラウザの
localStorage に保存されます。サーバーもビルド手順もありません。

---

# 就業規則

Claude はこのリポジトリで作業するとき、以下を必ず守ること。

## 1. 消す・戻す操作の前は、現状を見せて確認を取る

ファイルやフォルダを **消す・上書きする・元に戻す** 操作をする前は、
**必ず作業を止めて、今どうなっているかを先に見せ、持ち主の「OK」をもらってから実行する。**

対象になる操作(例):

- ファイル・フォルダの削除(`rm`、`git clean` など)
- ファイルの上書き・中身の全入れ替え
- 変更の取り消し・巻き戻し(`git checkout --`、`git restore`、`git reset --hard`、
  `git revert`、force push など)

確認を取るときは、次の3つをセットで見せる:

1. **何が対象か** — 消える/戻るファイル名の一覧
2. **今の状態** — そのファイルの中身、または `git status` の結果
3. **実行したらどうなるか** — 「この編集内容は元に戻せません」など、失われるものを一言で

持ち主が「OK」と言うまで実行しない。急いでいそうに見えても、勝手に判断して進めない。

## 2. 大事なフォルダは、頼まれても消さない

以下は **保護対象**。持ち主から「消して」と言われても、**その場では消さない。**

<!-- 保護したいフォルダ・ファイルをここに追記してください -->

- (未指定 — 持ち主が指定したらここに追記する)
- `.git/`(リポジトリの全履歴。消すと過去の記録がすべて失われる)
- タイムロガーからエクスポートした JSON バックアップファイル

保護対象を消す依頼が来たときの対応:

1. まず断る。「ここは保護対象なので、この場では消しません」と伝える
2. **なぜ危ないか**(何が失われるか)を一言で説明する
3. 代わりにできること(バックアップを取ってから、一部だけ移動する、など)を提案する
4. それでも持ち主が「本当に消していい」とはっきり言った場合のみ、
   バックアップを取ってから実行する

## 3. よく分からない操作の許可を求めるときは、一言の説明をつける

許可を求めるとき、コマンドをそのまま貼るだけにしない。
**「これから何をするコマンドなのか」を、専門用語なしの一言で必ず添える。**

- ❌ `git reset --hard HEAD~1` を実行してよいですか?
- ⭕ 直前の変更を丸ごと取り消して、1つ前の状態に戻します(取り消した内容は元に戻せません)。
  実行してよいですか?

書き方の目安:

- 専門用語(コミット、ステージ、リベース等)は、使うなら普通の言葉で言い換える
- 元に戻せない操作なら、**その一言に「元に戻せません」を必ず入れる**
- 説明は1〜2行。長くしない
